Place generated Deep Zoom output here.

Expected files:
- `api-design.dzi`
- `product-design.dzi`
- `full-blueprint.dzi`

Each `.dzi` file should have a matching `*_files/` tile directory beside it.

